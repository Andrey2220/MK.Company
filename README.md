# MK.COMPANY — Адаптивный сайт строительной компании

Чистый, минималистичный сайт на HTML, CSS и JavaScript с Node.js backend для обработки заявок.

## 📁 Структура проекта

```
Site/
├── index.html                 # Главная страница
├── services.html              # Страница услуг
├── team.html                  # Страница команды
├── contact.html               # Страница контактов и формы
├── css/
│   └── style.css              # Адаптивные стили (мобайл, планшет, десктоп)
├── js/
│   └── main.js                # Мобильное меню + обработка формы
├── server/
│   ├── index.js               # Express сервер с endpoint /api/contact
│   ├── package.json           # Зависимости
│   ├── .env.example           # Шаблон переменных окружения
│   └── README.md              # Инструкции сервера
└── README.md                  # Этот файл
```

## 🎨 Дизайн

- **Цветовая схема**: Белый фон, чёрный текст, золотой акцент (#E0A437)
- **Типографика**: Системные шрифты (Arial, Roboto, Helvetica)
- **Адаптивность**: Mobile-first подход
  - Desktop (1200px+): Полная сетка и навигация
  - Планшет (768px–1199px): 1–2 колонки, меню-аккордион
  - Мобайл (<768px): 1 колонка, hamburger меню

## 🚀 Быстрый старт

### 1. Запуск фронтенда

**Вариант A: Через простой HTTP-сервер (рекомендуется)**

```bash
# Перейди в папку Site
cd Site

# Запусти сервер на порту 5000
# На Windows (встроенный Python):
python -m http.server 5000

# На Mac/Linux:
python3 -m http.server 5000
```

Открой [http://localhost:5000](http://localhost:5000) в браузере.

**Вариант B: Через Node.js (http-server)**

```bash
npm install -g http-server
cd Site
http-server -p 5000
```

### 2. Настройка Node.js backend

```bash
# Перейди в папку server
cd Site/server

# Установи зависимости
npm install

# Скопируй шаблон .env
cp .env.example .env

# ⚠️ ВАЖНО: Заполни .env своими SMTP-данными (смотри ниже)
```

### 3. Заполнение .env (SMTP)

Отредактируй файл `server/.env` и заполни SMTP-параметры:

```env
PORT=3000
SMTP_HOST=smtp.yandex.ru          # или другой провайдер
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@yandex.ru    # твой email для отправки
SMTP_PASS=your-app-password       # пароль приложения (НЕ пароль аккаунта!)
TO_EMAIL=info@mk.company          # куда отправлять заявки
```

**Примеры SMTP по провайдерам:**

| Провайдер  | Host | Port | Secure | Примечание |
|------------|------|------|--------|-----------|
| Яндекс     | smtp.yandex.ru | 587 | false | Используй пароль приложения |
| Gmail      | smtp.gmail.com | 587 | false | Включи 2FA и создай пароль приложения |
| Mail.ru    | smtp.mail.ru | 587 | false | Пароль приложения |
| Hetzner   | smtp.yourdomain.com | 587 | false | Твой хостинг |

### 4. Запуск backend

```bash
cd Site/server
npm start
```

Сервер слушает на `http://localhost:3000`. Когда форма будет отправлена, письмо отправится на `TO_EMAIL`.

## ✅ Функционал

### Страницы
1. **Главная** — Hero с CTA, описание, 3 популярных услуги
2. **Услуги** — Сетка 6 услуг с описаниями
3. **Команда** — 6 карточек мастеров (фото-заглушка, имя, специализация)
4. **Контакты** — Форма с полями: имя, email, телефон, категория, сообщение

### Форма обратной связи
- **Поля**: Имя, Email*, Телефон, Категория (выпадающий список), Сообщение
- **Валидация**: Email обязателен на клиенте и сервере
- **Отправка**: POST на `http://localhost:3000/api/contact`
- **Ответ**: JSON с `{ ok: true }` или ошибкой
- **Уведомление**: Зелёное сообщение на 5 сек после успешной отправки

## 🛠 Дальнейшее развитие

- **Логотип**: Замени текст в `.brand` на `<img src="logo.svg">`
- **Контакты**: Отредактируй телефон/email в футере (есть в каждом HTML)
- **Описания**: Обнови тексты в разделах услуг и команды
- **Изображения**: Замени placeholder URLs на реальные картинки
- **Дополнительное**: Добавь карту (Google Maps), видео, аналитику (GA)

## 🔐 Безопасность

- **Никогда** не коммитьте `.env` в git — он в `.gitignore`
- **Скрывай** SMTP-пароли (используй переменные окружения)
- Для боевого сервера используй **HTTPS** и **Rate Limiting**

## 📱 Тестирование на мобилке

1. На локальной машине открой: `http://localhost:5000`
2. На телефоне откройте: `http://<твой IP>:5000`
   (Узнай IP через `ipconfig` на Windows или `ifconfig` на Mac/Linux)
3. Проверь: меню, карточки, форму на разных размерах

---

**Готово к начинке контента и деплою!** 🚀
